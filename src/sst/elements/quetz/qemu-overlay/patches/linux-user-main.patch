--- a/linux-user/main.c
+++ b/linux-user/main.c
@@ -1,6 +1,7 @@
 #include "qemu.h"
 #include "qemu/osdep.h"
 #include "qemu-version.h"
+#include "sst_mmio.h"
 #include "qemu/help_option.h"
 #include "qemu/error-report.h"
 #include "qemu/module.h"
@@ -200,6 +201,7 @@ static const struct qemu_argument arg_table[] = {
     {"d", "QEMU_LOG", false, QEMU_OPTION_log,
      "log", "log"},
+    {"sst-mmio-range", "SPEC", true, QEMU_OPTION_sst_mmio_range,
+     "sst-mmio-range", "Quetz MMIO range shmname=,base=,size= (repeatable)"},
 #define QEMU_OPTIONS
 #include "qemu-options.def"
 #undef QEMU_OPTIONS
@@ -400,6 +402,10 @@ int main(int argc, char **argv, char **envp)
             case QEMU_OPTION_log:
                 optarg = optarg;
                 break;
+            case QEMU_OPTION_sst_mmio_range:
+                sst_mmio_register_range(optarg);
+                break;
             case QEMU_OPTION_Q:
             default:
                 /* handled in qemu-options.def */
@@ -900,4 +908,5 @@ int main(int argc, char **argv, char **envp)
     }
     target_cpu_reset(cpu);
+    sst_mmio_apply_mprotect();
     if (gdbstub) {
         gdbserver_start(gdbstub_port);
